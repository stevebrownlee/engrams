---
import DocsLayout from '../../../layouts/DocsLayout.astro';
---

<DocsLayout title="How Engrams Works" description="Architecture and design of the Engrams memory platform.">
  <h1>How Engrams Works</h1>
  <p class="lead">
    Every time you start a new chat with your AI assistant, it has no memory of your previous conversations.
    Engrams changes that. It quietly sits alongside your AI tool, remembering your project decisions,
    design patterns, and progress — and automatically provides the right information when it's needed.
  </p>

  <h2>How a typical interaction works</h2>
  <ol>
    <li><strong>You ask your AI something.</strong> For example: "Add a /users endpoint."</li>
    <li><strong>Your AI checks with Engrams</strong> to see if there are any relevant decisions, patterns, or notes about this area of the project.</li>
    <li><strong>Engrams picks the best matches</strong> — only the most useful items, so the response stays fast and affordable.</li>
    <li><strong>Your AI writes code informed by real project context</strong> — not generic guesses.</li>
    <li><strong>New decisions get saved.</strong> As you work, Engrams records what you decide so it's there next time.</li>
  </ol>

  <h2>Storage</h2>
  <p>
    Each project gets its own local database stored in a <code>engrams/</code> folder at your project root.
    Your data is never shared between projects, and the database is created automatically the first time you use Engrams.
  </p>

  <h2>Visibility levels</h2>
  <p>
    Every item stored in Engrams — decisions, patterns, custom data — carries an optional
    <strong>visibility</strong> label that controls who sees it and whether it is shared with
    your team.
  </p>
  <table>
    <thead><tr><th>Visibility</th><th>Meaning</th></tr></thead>
    <tbody>
      <tr><td><code>team</code></td><td>Shared across all teammates. Exported and committed to Git.</td></tr>
      <tr><td><code>individual</code></td><td>Personal notes or local state. Never committed.</td></tr>
      <tr><td><code>proposed</code></td><td>Suggestions under review before becoming team decisions.</td></tr>
      <tr><td><code>workspace</code></td><td>Workspace-wide defaults visible to anyone using this repo.</td></tr>
    </tbody>
  </table>

  <h2>Team sync via Git</h2>
  <p>
    Because the Engrams database is a binary SQLite file, Git cannot 3-way merge it when two
    teammates both push changes. Engrams solves this with a <strong>markdown round-trip</strong>:
  </p>
  <ol>
    <li>A <strong>pre-commit hook</strong> exports <code>visibility=team</code> items to a versioned
        <code>engrams-export/</code> directory and stages the markdown files automatically.</li>
    <li>A <strong>post-merge hook</strong> imports those markdown files back into every teammate's
        local database after each <code>git pull</code> or <code>git merge</code>.</li>
    <li>The import uses <strong>merge mode</strong> — it only adds new items, so individual notes
        and personal progress are never overwritten.</li>
  </ol>
  <p>
    Decisions are identified across machines by a <strong>content-addressed slug</strong>
    (a 12-character SHA-256 of the summary text), so the same decision exported from Alice's
    machine is recognised as identical on Bob's machine and skipped rather than duplicated.
  </p>
  <p>
    Run <code>engrams install-hooks</code> once per repository to wire everything up automatically.
    See the <a href="/docs/features/batch-export">Batch &amp; Export</a> page for details.
  </p>

  <h2>Workspace detection</h2>
  <p>
    Engrams figures out which project you're working in automatically by looking for common project
    files like <code>.git</code>, <code>package.json</code>, <code>pyproject.toml</code>, or <code>Cargo.toml</code>.
    You never need to tell it where your project lives.
  </p>

  <h2>How Engrams connects to your AI tool</h2>
  <table>
    <thead><tr><th>Mode</th><th>What it does</th><th>Best for</th></tr></thead>
    <tbody>
      <tr>
        <td><strong>Local (stdio)</strong></td>
        <td>Runs directly alongside your AI tool as a local process</td>
        <td>IDE extensions like Roo Code, Cline, Cursor, and Windsurf</td>
      </tr>
      <tr>
        <td><strong>Network (HTTP)</strong></td>
        <td>Runs as a web server that clients can connect to</td>
        <td>Remote setups, or multiple people sharing one Engrams instance</td>
      </tr>
    </tbody>
  </table>
  <p>Most users will use <strong>Local (stdio)</strong> — it's the default and requires no extra configuration.</p>

  <h2>Smart search (semantic search)</h2>
  <p>
    When you save a decision or pattern, Engrams also creates a compact representation of its
    <em>meaning</em>. Later, when your AI searches for something like "caching strategy," Engrams can
    find related items — even if they use completely different words, like "Redis configuration"
    or "query performance." This is called <strong>semantic search</strong>, and it's what makes Engrams
    feel like it actually understands your project.
  </p>

  <h2>Keyword search</h2>
  <p>
    Engrams also supports fast, traditional keyword search. If you know the exact term you're
    looking for — like a specific library name or error message — keyword search finds it
    instantly without any extra processing.
  </p>

  <h2>The knowledge graph</h2>
  <p>
    Your project knowledge isn't isolated facts — it's all connected. Engrams lets you link
    related items together. For example, you can connect a design decision to the coding patterns
    that implement it, or link a task to the decisions it depends on. Your AI can follow these
    connections to understand the bigger picture.
  </p>

  <h2>Context budgeting</h2>
  <p>
    As your project grows, you might have hundreds of saved decisions and patterns. Loading all
    of them into every AI conversation would be slow and expensive. Engrams solves this by ranking
    items by how relevant they are to your current question and only including the most useful
    ones. The result: fast, focused, affordable responses. See
    <a href="/docs/features/budgeting">Context Budgeting</a> for details.
  </p>
</DocsLayout>

<style>
  .lead { font-size: 1.125rem; color: var(--color-text-muted); line-height: 1.75; margin-bottom: 1.75rem; }
</style>
